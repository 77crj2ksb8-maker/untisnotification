# Bewusst schlank -- je weniger Abhaengigkeiten, desto weniger kann brechen.
requests>=2.32,<3
webuntis>=0.1.24

# Nur fuer die Pruefungen (pytest und ruff), nicht fuer den Betrieb. Sie
# stehen trotzdem hier statt in einer zweiten Datei: Eine Datei weniger ist
# mehr wert als die zwei Sekunden, die der Bot-Job laenger installiert.
pytest>=8.0
ruff>=0.5

# Nur damit die Tests die Workflow-Dateien lesen koennen. Ein Syntaxfehler
# im run-Block eines Workflows faellt sonst erst im Betrieb auf -- und dort
# ausgerechnet doppelt, weil auch der Meldeschritt ein run-Block ist und am
# selben Fehler stirbt. Genau das ist in 2.3.0 passiert.
pyyaml>=6.0
